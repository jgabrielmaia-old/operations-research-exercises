# Modelagem
# Variaveis de decisao: se o problema estivesse resolvido, qual seria o aspecto da solucao?
# funcao-objetivo: meta, objetivo do problema, grandeza a otimizar
# restricoes: o que eh possivel fazer, limites minimos e maximos que restringem a solucao

var x1 >= 0;
var x2 >= 0;
var x3 >= 0;
var x4 >= 0;

minimize z:     x1 + 0.25 * x2 + 4 * x3 + 1.5 * x4;

subject to c11: 1000 * x1 + 300 * x2 + 2400 * x3 + 800 * x4 >= 700;
subject to c12:                   x2                        >= 0.25;
subject to c13:                               x3            >= 0.15;
subject to c14:        x1  +      x2 +        x3  +      x4  = 1;

end;